<div class="container mt-5">

<h1>New Review</h1>
<%= simple_form_for [@restaurant, @review] do |r| %>
  <%= r.input :content %>
  <%= r.input :rating %>
  <%= r.submit "ADD", class: "btn btn-primary" %>
<% end %>
</div>
